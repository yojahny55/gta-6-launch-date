# GTA 6 Tracker - Environment Variables Template
# Copy this file to .env or .dev.vars for local development
# For production, set these in Cloudflare Dashboard → Workers → Settings → Variables

# Cloudflare Account Configuration
# Where to obtain: Cloudflare Dashboard → Workers → Overview
CLOUDFLARE_ACCOUNT_ID=your_account_id_here

# Cloudflare API Token
# Where to obtain: Cloudflare Dashboard → My Profile → API Tokens
# Required permissions: Workers Scripts Edit, D1 Edit
CLOUDFLARE_API_TOKEN=your_api_token_here

# D1 Database Name
# Where to obtain: Created in Story 1.2 via `wrangler d1 create gta6-predictions`
# This will be configured in wrangler.toml after database creation
DATABASE_NAME=gta6_predictions_db

# Google reCAPTCHA v3 (for bot protection)
# Where to obtain: https://www.google.com/recaptcha/admin
# Select reCAPTCHA v3 and register your domain
RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here
RECAPTCHA_SECRET_KEY=your_recaptcha_secret_key_here

# Google AdSense (for monetization)
# Where to obtain: https://www.google.com/adsense
# Sign up and get your publisher ID
ADSENSE_CLIENT_ID=ca-pub-XXXXXXXXXXXXXXXX

# IP Hashing Salt (for privacy-preserving rate limiting)
# Generate a random string for this value
# Example: openssl rand -hex 32
IP_HASH_SALT=your_random_salt_here_generate_with_openssl
